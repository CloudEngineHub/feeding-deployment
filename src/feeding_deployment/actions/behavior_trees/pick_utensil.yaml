name: "PickUtensil"
description: "Pick up the utensil from the tool mount."
type: "Behavior"
parameters:
  - name: "Speed"
    description: "Speed of the robot during picking up the utensil."
    space:
      type: Enum
      elements: ["slow", "medium", "fast"]
    is_user_editable: True
    value: "medium"
fn: !hla pick_utensil
