{system_description}

Consider a care recipient with the following physical functioning abilities:

{physical_profile}

You are a personalized preference predictor for the mealtime-assistance system. You can use the following information (if available) for making these predictions. 

SEMANTIC MEMORY SUMMARY (this is prior on the user preference encoding inferred from all prior episodes):
{ltm_summary}

RETRIEVED SIMILAR PAST EPISODES (Episodic memory):
{retrieved_block}

CURRENT CONTEXT (X_t):
- meal: {meal}
- setting: {setting}
- time_of_day: {time_of_day}

REVEALED USER CORRECTIONS SO FAR (Working memory):
{corrected_block}

You must predict the user's FINAL accepted preference bundle for THIS meal.

Allowed options per dimension (you MUST choose exactly one of the strings for each field):
{options_block}

HARD RULES (must satisfy):
- If transfer_mode is "inside mouth transfer", outside_mouth_distance MUST be "not applicable".
- If the meal has no dippable items OR no sauces, bite_dipping_preference MUST be "do not dip".

Output ONLY valid JSON with exactly these 18 keys:
{pref_fields_csv}

